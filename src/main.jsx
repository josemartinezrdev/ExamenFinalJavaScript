import React from "react";
import ReactDOM from "react-dom/client";
import { MenuComponent } from "./web-components/menu-component";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MenuComponent />
  </React.StrictMode>
);
